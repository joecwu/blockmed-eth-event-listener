{
  "description": "blockmed transaction pipeline",
  "processors": [
    {
      "set": {
        "field": "_id",
        "value": "{{uid}}"
      }
    },
    {
      "script": {
        "source": " ctx._index = 'blockmed-trans-' + (ctx.blockNumber/100000)"
      }
    }
  ]
}